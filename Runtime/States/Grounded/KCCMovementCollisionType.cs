namespace GalaxyGourd.KCC
{
    public enum KCCMovementCollisionType
    {
        None,
        Front,  // When the KCC is colliding iwth something in front of it
        Back    // When the KCC is colliding iwth something behind it
    }
}